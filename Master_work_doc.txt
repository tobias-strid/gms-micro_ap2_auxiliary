Per Sikora och Tomas SnŠckerstršm coordinates

1. Uploading data
	a. Automatic uploads (cron, trigger)
	b. Parsing of data from instruments Ð jsons, xmls 
	c. Define for paths to sequencing data
	d. Metadata Sanity Check
	e. Latest version check
	f. Hook for running Petagene compression
	g. Hook for pulling info from LIMS (separate singularity?)
	h. Include GET command to pull data from lake (w metadata search)
		i. Check format on different instruments and ensure compability
		i. IonTorrent
		ii. MiSeq
		iii. NextSeq
		iv. NovaSeq

2. Secondary analysis
	a. GET from the lake into cluster
	b. Automate launch analysis (MRSA)
		i. QC (fastQC-trimomatic-fastQC)
		ii. Map/VC/de-novo-assembly? (Use trimmed fastq discard after mapping, save raw fastq)
		iii. Singularity
		iv. Output run varibles into lake
		v. Pipeline result into lake
3. Tertiary Analysis
	a. Clustering
	b. Linking to historic data?
  c. Virtual PCR for SPA-typing?

4. Visualisation of analysed data
	a. Web interface connected to lake 

5. What metadata
	a. For folkan
	b. For smittskydd/kmik
	c. Document for raw data metadata tags (which ones to index first, which ones most relevant)
    i. Run metadata automatic from log file
    but also
    ii. Submitting GMC
    iii. Sample type (bacterial MRSA)
    iv. library prep protocol
    
  d. Pipeline related Metadata
    i. date of analysis
    ii. pipeline version 
    iii. QC-data from pipeline
6. GitWiki Documentation

